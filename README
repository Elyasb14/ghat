GHAT

this is a multi-user terminal chat app. all traffic is over pure tcp, no encryption. beware!

USAGE

to start a server, run:

```
go run cmd/server/main.go --port 8080
```

to connect to a server, run the following:

```
go run cmd/client/main.go --addr <remote addr>
```
